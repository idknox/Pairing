= link_to "New Template", new_assessments_quiz_template_path, class: 'action', style: 'float: right;'

%h1 Quiz Templates

- @quiz_templates.each do |quiz_template|
  .quiz_template
    = link_to quiz_template.name, assessments_quiz_template_path(quiz_template)
    &ndash;
    = pluralize quiz_template.questions.length, 'question'