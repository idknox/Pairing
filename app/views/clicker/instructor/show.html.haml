%section.clicker.instructor
  %h1 Lesson Overview

  %p(data-container="student-summary")

  %div.students{data: {behavior: 'student-container'}}
  %a(href="/clicker/#{params[:location]}/instructor/reset" data-remote-custom="true") Reset

- content_for :javascripts do
  :javascript
    $(function(){
      pusher = new Pusher('#{ENV["PUSHER_KEY"]}');

      InstructorApp.setup(pusher, "#{params[:location]}");
    });
