%h2 Apply!
= form_for [@application.job_opportunity, @application] do |f|
  = f.label :resume
  = f.file_field :resume

  = f.label :cover_letter
  = f.file_field :cover_letter
  = f.submit "Apply for this position", class: "action"
