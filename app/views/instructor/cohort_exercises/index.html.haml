.sub-nav
  %h1= @cohort.name

  = render partial: 'instructor/cohorts/instructor_cohort_sub_nav', locals: { cohort_id: params[:cohort_id] }

  .content
    .action-table
      %table
        %thead
          %tr
            %th Exercise Name
        %tbody
          - @exercises.each do |exercise|
            %tr
              %td= link_to exercise.name, instructor_cohort_cohort_exercise_path(@cohort, exercise)
      .actions
        = link_to "Assign Exercise", new_instructor_cohort_cohort_exercise_path
